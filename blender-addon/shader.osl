shader simple_material(
    output closure color BSDF = emission()
){
    
    color material_color = P;

    color fov = color(0,0,0);
    getattribute("object:location", fov);

    BSDF = fov * emission();
}